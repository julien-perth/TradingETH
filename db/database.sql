CREATE TABLE etherum (
    id INTEGER PRIMARY KEY,
    info_date DATE,
    prices VARCHAR(25),
    volume VARCHAR(25),
    candle_closed VARCHAR(25),
    price_variation VARCHAR(25),
    total_price_variation VARCHAR(25)
);